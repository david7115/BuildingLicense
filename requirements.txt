# 👉 필수 라이브러리
streamlit==1.35.0         # Streamlit 웹앱 프레임워크
pandas==2.2.2             # 데이터프레임 처리
openpyxl==3.1.2           # Excel 파일(xlsx) 입출력 엔진

# 👉 선택 라이브러리 (선택에 따라 포함 가능)
# numpy==1.26.4           # 수치 계산 라이브러리 (필요시)
# matplotlib==3.8.4       # 그래프 시각화 (필요시)
# plotly==5.21.0          # 동적 시각화 대안
# xlrd==2.0.1             # Excel 읽기(xls) 지원
# PyYAML==6.0.1           # 설정파일 처리

# 👉 UTF-8 인코딩 유지용 설정 (Windows 대비 호환성 고려 시)
# chardet==5.2.0
